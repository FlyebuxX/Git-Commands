remove a file from the staging area : git rm --cached <file name>

creating a new git branch : git checkout -b <branch name>

move from a branch to another : git checkout <branch name>

show every branches : git branch

téléverser une branche sur la branche master : 1) se situer sur la branche master
						2) git merge <branch name> à fusionner


git branch -M main
git remote add origin <ulr>
git push -u origin main

git push origin <branch name> : push branch


git clone : copy url from github / git clone <url>
